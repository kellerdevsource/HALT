using UnityEngine;
using System.Collections;

public class SphereShadowRotation : MonoBehaviour {

	// Use this for initialization
	
	// Update is called once per frame
	void Update () {
		transform.forward = Vector3.down;
	}
}
